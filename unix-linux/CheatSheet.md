# Linux Cheat Sheet

## Disk / Storage

```bash
lsblk           # List disks and partitions
lsblk -f        # Show filesystems and mount points
df -h           # Show free/used disk space
du -sh <dir>    # Show directory size
sudo fdisk -l   # Show partition table details
```
